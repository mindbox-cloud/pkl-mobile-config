import "../_SettingsInAppBase.pkl" as base

output {
  renderer = new JsonRenderer {}
  value = base.settings.toMap()
    .mapValues((key, value) ->
      if (key == "featureToggles") value.toMap()
        .mapValues((k, v) -> if (k == "MobileSdkShouldSendInAppShowError") "yes" else v)
        .toDynamic()
      else value
    )
    .toDynamic()
}
