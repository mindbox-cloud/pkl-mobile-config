import "../_SettingsBase.pkl" as base

output {
  renderer = new JsonRenderer {}
  value = base.settings.toMap()
    .mapValues((key, value) ->
      if (key == "ttl") value.toMap().mapKeys((k, _) -> if (k == "inapps") "inappsTest" else k).toDynamic()
      else value
    )
    .toDynamic()
}
