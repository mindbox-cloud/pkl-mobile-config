import "_InAppBase.pkl" as base

output {
  renderer = new JsonRenderer { omitNullProperties = false }
}

inapps = new Listing {
  base.validInAppMap
    .mapValues((key, value) -> if (key == "form") 1 else value)
    .toDynamic()
}
