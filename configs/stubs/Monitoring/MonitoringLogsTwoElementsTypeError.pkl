import "../../../templates/MonitoringTemplate.pkl"

logs = new Listing {
  MonitoringTemplate.logs[0].toMap()
    .mapValues((key, value) -> if (key == "requestId") 1 else value)
    .toDynamic()
  MonitoringTemplate.logs[1].toMap()
    .mapValues((key, value) -> if (key == "from") new Dynamic {} else value)
    .toDynamic()
}
