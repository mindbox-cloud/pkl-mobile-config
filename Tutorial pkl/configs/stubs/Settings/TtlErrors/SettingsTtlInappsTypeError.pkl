import "../_SettingsBase.pkl" as base

output {
  renderer = new JsonRenderer {}
  value = base.settings.toMap()
    .mapValues((key, value) ->
      if (key == "ttl") value.toMap().mapValues((k, v) -> if (k == "inapps") 123 else v).toDynamic()
      else value
    )
    .toDynamic()
}
