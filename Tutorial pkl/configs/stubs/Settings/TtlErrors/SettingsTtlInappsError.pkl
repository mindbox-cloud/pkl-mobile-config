import "../_SettingsBase.pkl" as base

output {
  renderer = new JsonRenderer {}
  value = base.settings.toMap()
    .mapValues((key, value) ->
      if (key == "ttl") new Dynamic { inappsTest = "1.00:00:00" }
      else value
    )
    .toDynamic()
}
