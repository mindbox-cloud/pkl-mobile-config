import "../_SettingsInAppBase.pkl" as base

output {
  renderer = new JsonRenderer {}
  value = base.settings.toMap()
    .mapValues((key, value) ->
      if (key == "inapp") value.toMap().filter((k, _) -> k != "minIntervalBetweenShows").toDynamic()
      else value
    )
    .toDynamic()
}
